name: keep-render-awake
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl health
        run: curl -fsS "$PING_URL" || true
        env:
          PING_URL: ${{ secrets.PING_URL }}
