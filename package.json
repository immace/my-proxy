{
  "name": "my-proxy",
  "version": "1.0.0",
  "description": "Sphere demo server (static UI + reverse proxy via upstream proxy)",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "proxy-agent": "^6.3.1"
  }
}
